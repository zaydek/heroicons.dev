{
	"name": "heroicons.dev",
	"scripts": {
		"tailwind-dev": "tailwind build -o src/stylesheets/tailwindcss/tailwind.generated.css",
		"tailwind-build": "NODE_ENV=production yarn tailwind-dev",
		"dev": "next dev",
		"build": "next build",
		"out": "rm -rf .next && yarn tailwind-build && yarn build && yarn next export && osascript -e 'display notification \"Production build ready\" with title \"yarn compile\"' && yarn tailwind-dev && osascript -e 'display notification \"Development build ready\" with title \"yarn tailwind-dev\"'",
		"start": "next start"
	},
	"dependencies": {
		"immer": "latest",
		"lodash": "latest",
		"next": "^9.5.3",
		"react": "latest",
		"react-dom": "latest",
		"use-immer": "latest"
	},
	"devDependencies": {
		"@tailwindcss/ui": "latest",
		"babel-eslint": "latest",
		"babel-jest": "latest",
		"eslint": "latest",
		"jest": "latest",
		"tailwindcss": "latest"
	}
}
